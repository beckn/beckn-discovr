appName: "onix-crawler-local"
log:
  level: debug
  destinations:
    - type: stdout
  contextKeys:
    - transaction_id
    - message_id
    - subscriber_id
    - module_id
http:
  port: 8081
  timeout:
    read: 30
    write: 30
    idle: 30
pluginManager:
  root: ./plugins

# Crawl-only module. schemaValidator is intentionally omitted (optional in the
# crawl handler) — the ingestion service validates /push downstream, so we skip
# the beckn.yaml fetch inside the crawler. DB DSN, push endpoint, and index URL
# come from env in the compose service (env overrides any value here).
modules:
  - name: crawl
    path: /crawl
    handler:
      type: crawl
      role: bap
      httpClientConfig:
        maxIdleConns: 1000
        maxIdleConnsPerHost: 200
        idleConnTimeout: 300s
        responseHeaderTimeout: 5s
      plugins:
        crawler:
          id: catalogcrawler
          config:
            CRAWLER_NETWORK_IDS: ""
            CRAWLER_INDEX_INTERVAL: "1m"
            CRAWLER_CATALOG_INTERVAL: "15s"
            CRAWLER_FETCH_TIMEOUT: "30s"
            CRAWLER_MAX_ARTIFACT_BYTES: "10485760"
            CRAWLER_MAX_DECOMPRESSED_BYTES: "104857600"
            CRAWLER_MAX_PUSH_BYTES: "10485760"
            CRAWLER_MAX_ATTEMPTS: "5"
            CRAWLER_MERGE_ONLY: "true"
